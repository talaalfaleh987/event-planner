import { DropdownToggle } from './dropdown-toggle';

describe('DropdownToggle', () => {
  it('should create an instance', () => {
    const directive = new DropdownToggle();
    expect(directive).toBeTruthy();
  });
});
